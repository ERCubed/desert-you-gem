# To Do List

- Figure out why `DesertYou::Namespace` does not work.
- Add configuration to allow for different formats
  - `:default`, `:metal`, `:folk`, etc...
  - Ability to update the default to your own path
- Add rubocop, bump, etc...
- Add tests
